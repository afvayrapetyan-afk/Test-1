# 🔄 Автоматическое обновление данных - Руководство

## 📊 Как работает система обновления

### 🤖 Два AI-агента работают автоматически:

#### 1️⃣ **TrendScoutAgent** - Поиск трендов
- **Частота:** Каждые 6 часов (00:00, 06:00, 12:00, 18:00)
- **Что делает:** Сканирует Reddit, Google Trends и другие источники
- **Результат:** Находит 10 новых трендов за запуск
- **Категории:** technology, business, ai, saas, fintech

#### 2️⃣ **IdeaAnalystAgent** - Генерация бизнес-идей
- **Частота:** Каждые 12 часов (00:30, 12:30)
- **Что делает:** Анализирует неизученные тренды
- **Результат:** Генерирует 1-2 бизнес-идеи на каждый тренд
- **Фильтр:** Анализирует только новые тренды (которые еще не обработаны)

---

## 📈 Правила генерации идей

### ✅ Какие идеи попадают в систему:

**6-метричная система оценки (каждая метрика от 0 до 100):**
1. **Market Size** - Размер рынка (потенциальная аудитория)
2. **Competition** - Уровень конкуренции (меньше = лучше)
3. **Demand** - Спрос и актуальность
4. **Monetization** - Потенциал монетизации
5. **Feasibility** - Реализуемость (технически и ресурсно)
6. **Time to Market** - Скорость выхода на рынок

**Общий рейтинг (Total Score):**
- Среднее арифметическое всех 6 метрик
- Идеи с рейтингом **≥ 75/100** считаются "топовыми"
- Идеи с рейтингом **< 50/100** отфильтровываются

### 🎯 Приоритеты:

**Высокий приоритет** (попадают в "Горячие тренды"):
- Total Score ≥ 75
- Market Size ≥ 80
- Demand ≥ 85

**Средний приоритет:**
- Total Score 60-74
- Показываются, но без метки "trending"

**Низкий приоритет:**
- Total Score < 60
- Сохраняются в БД, но не показываются в UI

---

## 🔢 Математика роста данных

### При автоматической работе:

**За день (4 цикла TrendScout + 2 цикла IdeaAnalyst):**
- Новых трендов: **40** (10 × 4 запуска)
- Новых идей: **~10** (5 трендов × 2 идеи каждые 12ч)

**За неделю:**
- Новых трендов: **~280**
- Новых идей: **~70**

**За месяц:**
- Новых трендов: **~1200**
- Новых идей: **~300**

### 🧠 Умная фильтрация:

**IdeaAnalystAgent анализирует только новые тренды:**
- Проверяет какие тренды уже обработаны
- Берет только **5 новых трендов** за цикл
- Это предотвращает дублирование идей

---

## ⚙️ Настройка автоматического обновления

### Вариант 1: Локальный запуск (для разработки)

**1. Установите Redis:**
```bash
# macOS
brew install redis
brew services start redis

# Linux
sudo apt install redis-server
sudo systemctl start redis
```

**2. Запустите Celery Worker:**
```bash
cd backend
celery -A app.tasks.scheduled_tasks worker --loglevel=info
```

**3. Запустите Celery Beat (планировщик):**
```bash
cd backend
celery -A app.tasks.scheduled_tasks beat --loglevel=info
```

**4. Готово!** Теперь агенты запускаются автоматически по расписанию.

---

### Вариант 2: Production (Railway/Render)

**На Railway:**

1. Добавьте Redis addon в ваш проект
2. Добавьте второй сервис для Celery Worker
3. Добавьте третий сервис для Celery Beat

**Variables для всех сервисов:**
```env
REDIS_URL=<railway-redis-url>
OPENAI_API_KEY=<your-key>
DATABASE_URL=<your-db-url>
```

**Start Commands:**
- **Backend:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
- **Celery Worker:** `celery -A app.tasks.scheduled_tasks worker --loglevel=info`
- **Celery Beat:** `celery -A app.tasks.scheduled_tasks beat --loglevel=info`

---

## 🎮 Ручное управление

### Запустить агенты вручную (прямо сейчас):

```bash
cd backend
python demo.py
```

### Через API (запланировано):

```bash
# Запустить поиск трендов
curl -X POST http://localhost:8000/api/v1/agents/run \
  -H "Content-Type: application/json" \
  -d '{"agent": "trend_scout", "params": {"limit": 10}}'

# Запустить анализ идей
curl -X POST http://localhost:8000/api/v1/agents/run \
  -H "Content-Type: application/json" \
  -d '{"agent": "idea_analyst"}'
```

---

## 💰 Стоимость автоматической работы

### OpenAI API costs:

**За один цикл:**
- TrendScoutAgent: ~$0.05-0.10 (10 трендов)
- IdeaAnalystAgent: ~$0.15-0.25 (5 идей)

**За день:** ~$0.80-1.50
**За месяц:** ~$25-45

### 💡 Оптимизация расходов:

1. **Уменьшить частоту:**
   - Тренды: с 6ч → 12ч (экономия 50%)
   - Идеи: с 12ч → 24ч (экономия 50%)

2. **Уменьшить количество:**
   - Тренды: с 10 → 5 (экономия 50%)
   - Идеи: с 5 → 3 (экономия 40%)

3. **Использовать GPT-3.5-turbo вместо GPT-4:**
   - Экономия ~90%, но качество ниже

---

## 📝 Логирование и мониторинг

### Где смотреть логи:

**Локально:**
```bash
tail -f celery.log
```

**Production (Railway):**
- Dashboard → Deployments → Logs

### Важные события:

- ✅ `Trends discovered: X` - найдено трендов
- ✅ `Ideas generated: X` - создано идей
- 💰 `Cost: $X` - затраты на OpenAI
- ❌ `Error: ...` - ошибки выполнения

---

## 🔍 Monitoring Dashboard (будущее)

Планируется добавить:
- График роста идей в реальном времени
- Статистика по категориям трендов
- AI cost tracker
- Успешность идей (какие стали бизнесами)

---

## ❓ FAQ

**Q: Почему только 6 идей после первого запуска?**
A: `demo.py` анализирует только 3 тренда для экономии OpenAI credits. Автоматическая система будет генерировать ~10 идей в день.

**Q: Могут ли быть дубликаты идей?**
A: Нет, IdeaAnalystAgent проверяет какие тренды уже обработаны и анализирует только новые.

**Q: Как удалить старые/плохие идеи?**
A: Сейчас вручную через БД. В будущем добавим UI для архивации идей с рейтингом < 60.

**Q: Можно ли настроить свои категории трендов?**
A: Да! Измените `categories` в `scheduled_tasks.py` строка 42.

**Q: Работает ли это без Celery?**
A: Да, можно запускать `demo.py` вручную или через cron, но Celery удобнее для production.

---

**Готово!** 🚀 Теперь ваша система будет автоматически находить тренды и генерировать бизнес-идеи 24/7.
