# 🚀 AI Business Portfolio Manager - Quick Start

Добро пожаловать! Ваш AI-менеджер бизнес-портфеля готов к работе!

## ✅ Что уже работает

Система **полностью функциональна** и включает:

- ✅ **Backend API** (FastAPI) на порту 8000
- ✅ **SQLite база данных** с автоинициализацией
- ✅ **AI Агенты**:
  - **TrendScoutAgent** - находит тренды (AI-генерация + Reddit scraping)
  - **IdeaAnalystAgent** - анализирует тренды и генерирует бизнес-идеи
- ✅ **OpenAI интеграция** - GPT-4o и GPT-4o-mini
- ✅ **REST API** с полной документацией (Swagger UI)
- ✅ **Демо-скрипт** для тестирования

## 🎯 Быстрый запуск (3 минуты)

### 1. Запустите Backend

```bash
cd "/Users/vardanajrapetan/Project 1/backend"
python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**Backend запущен!** 🎉

### 2. Откройте Swagger UI

Перейдите в браузере: **http://localhost:8000/docs**

Здесь вы можете:
- Посмотреть все API endpoints
- Протестировать запросы
- Изучить форматы данных

### 3. Запустите AI агентов для генерации данных

```bash
cd "/Users/vardanajrapetan/Project 1/backend"
python3 demo.py
```

Это создаст:
- **5 трендов** из разных категорий (AI, SaaS, marketplace и т.д.)
- **3 бизнес-идеи** с полным анализом по 6 метрикам
- Полную статистику и стоимость операций

## 📊 Просмотр данных

### Через API

**Все тренды:**
```bash
curl http://localhost:8000/api/v1/trends/ | python3 -m json.tool
```

**Все бизнес-идеи:**
```bash
curl http://localhost:8000/api/v1/ideas/ | python3 -m json.tool
```

**История выполнения агентов:**
```bash
curl http://localhost:8000/api/v1/agents/executions/ | python3 -m json.tool
```

### Через Swagger UI

Лучший способ! Откройте: **http://localhost:8000/docs**

## 🤖 Как запустить AI агентов вручную

### Через Swagger UI (рекомендуется)

1. Откройте http://localhost:8000/docs
2. Найдите `/api/v1/agents/run`
3. Нажмите "Try it out"
4. Используйте примеры ниже

### Пример 1: TrendScoutAgent (поиск трендов)

```json
{
  "agent_type": "trend_scout",
  "params": {
    "sources": ["reddit"],
    "limit": 10
  }
}
```

### Пример 2: IdeaAnalystAgent (анализ трендов)

```json
{
  "agent_type": "idea_analyst",
  "params": {
    "trend_ids": [1, 2, 3]
  }
}
```

## 📁 Структура проекта

```
Project 1/
├── backend/                    # Backend API
│   ├── app/
│   │   ├── agents/            # AI Агенты
│   │   │   ├── trend_scout_agent.py    # Поиск трендов
│   │   │   ├── idea_analyst_agent.py   # Анализ идей
│   │   │   └── base_agent.py           # Базовый класс
│   │   ├── modules/
│   │   │   ├── trends/        # Тренды: models, schemas, service, router
│   │   │   ├── ideas/         # Идеи: models, schemas, service, router
│   │   │   └── agents/        # Agent executions
│   │   ├── scrapers/          # Скраперы данных
│   │   │   ├── reddit_scraper.py       # Reddit (PRAW)
│   │   │   └── base_scraper.py
│   │   ├── core/
│   │   │   ├── config.py      # Настройки (OpenAI API key и т.д.)
│   │   │   └── database.py    # SQLAlchemy setup
│   │   └── main.py            # FastAPI app
│   ├── demo.py                # Демо-скрипт
│   ├── business_portfolio.db  # SQLite база
│   └── .env                   # Переменные окружения
└── docs/                      # Документация
```

## 💰 Стоимость использования

**Демо (5 трендов + 3 идеи):** ~$0.02

**Разбивка:**
- TrendScoutAgent: $0.0003 (GPT-4o-mini)
- IdeaAnalystAgent: $0.019 (GPT-4o)

**Совет:** Используйте GPT-4o-mini для массовых операций, GPT-4o для важного анализа.

## 🔑 Настройка Reddit API (опционально)

Для реального scraping Reddit (вместо AI-генерации):

1. Прочитайте `docs/REDDIT_SETUP.md`
2. Создайте приложение на https://www.reddit.com/prefs/apps
3. Добавьте credentials в `.env`:
   ```
   REDDIT_CLIENT_ID=your-client-id
   REDDIT_CLIENT_SECRET=your-secret
   ```

Без этого система использует AI для генерации трендов (что тоже работает отлично!).

## 🌐 API Endpoints

Все endpoints доступны через **http://localhost:8000**

### Trends
- `GET /api/v1/trends/` - Список трендов
- `POST /api/v1/trends/` - Создать тренд
- `GET /api/v1/trends/{id}` - Получить тренд
- `POST /api/v1/trends/search` - Поиск трендов

### Ideas
- `GET /api/v1/ideas/` - Список идей
- `POST /api/v1/ideas/` - Создать идею
- `GET /api/v1/ideas/{id}` - Получить идею

### Agents
- `POST /api/v1/agents/run` - Запустить агента
- `GET /api/v1/agents/executions` - История запусков
- `GET /api/v1/agents/executions/{id}` - Детали выполнения
- `GET /api/v1/agents/status` - Статус агентов

## 📈 Метрики анализа бизнес-идей

Каждая идея оценивается по 6 метрикам (0-100):

1. **Market Size** - Размер рынка (TAM)
2. **Competition** - Уровень конкуренции (чем ниже, тем лучше)
3. **Demand** - Острота проблемы/спрос
4. **Monetization** - Потенциал монетизации
5. **Feasibility** - Техническая сложность (чем выше, тем проще)
6. **Time to Market** - Скорость запуска MVP

**Total Score** = среднее всех 6 метрик

## 🛠️ Следующие шаги

1. **Google Trends интеграция** - добавить pytrends для реальных трендов
2. **Frontend Dashboard** - красивый UI для просмотра данных
3. **Celery задачи** - асинхронное выполнение агентов
4. **Qdrant** - семантический поиск по трендам и идеям
5. **Telegram/VK scrapers** - для российского рынка

## 🚨 Troubleshooting

### Backend не запускается

```bash
# Проверьте порт
lsof -ti:8000

# Убейте процесс если занят
lsof -ti:8000 | xargs kill -9

# Перезапустите
cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

### OpenAI API ошибки

- Проверьте `.env` файл
- Убедитесь что API key действителен
- Проверьте баланс на https://platform.openai.com

### База данных ошибки

```bash
# Удалите и пересоздайте базу
cd backend
rm -f business_portfolio.db
python3 demo.py
```

## 📚 Документация

- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc
- **Health Check:** http://localhost:8000/health

## 🌍 Развертывание на Production

### Для показа людям используйте готовое руководство:

📖 **Читайте [DEPLOY_GUIDE.md](DEPLOY_GUIDE.md)** - полная инструкция по развертыванию

**Быстрое развертывание:**
1. **Backend на Railway:** railway.app (5 сервисов: API, PostgreSQL, Redis, Celery Worker, Celery Beat)
2. **Frontend на Vercel:** vercel.com
3. **Автообновление:** Каждый день в 9:00 UTC система автоматически находит новые тренды и генерирует идеи

**Ваша production ссылка:** https://test-1-iota-sepia.vercel.app

## 🎉 Готово!

Ваша система готова к использованию! Запустите `demo.py` и посмотрите как AI агенты генерируют бизнес-идеи.

**Удачи с вашим AI бизнес-портфелем!** 🚀
