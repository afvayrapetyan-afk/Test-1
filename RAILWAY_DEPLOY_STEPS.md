# 🚂 Пошаговое развертывание на Railway

## ✅ Что уже готово

1. ✅ Код закоммичен локально
2. ✅ Все конфигурационные файлы созданы
3. ✅ База данных с 10 идеями на русском языке
4. ✅ Автоматизация настроена (9:00 UTC ежедневно)

---

## 📤 Шаг 1: Загрузите код на GitHub (5 минут)

```bash
cd "/Users/vardanajrapetan/Project 1"

# Запушьте изменения
git push origin main
```

**Если просит пароль:**
- Используйте GitHub Personal Access Token вместо пароля
- Создайте token: https://github.com/settings/tokens
- Выберите: repo (full control)

**Или используйте GitHub Desktop:**
- Откройте проект в GitHub Desktop
- Нажмите "Push origin"

---

## 🚂 Шаг 2: Создайте проект на Railway (10 минут)

### 2.1 Войдите на Railway

1. Откройте: https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project"

### 2.2 Создайте сервисы

#### Сервис 1: PostgreSQL Database

1. Нажмите "+ New" → "Database" → "Add PostgreSQL"
2. Дождитесь создания
3. **ВАЖНО:** Скопируйте `DATABASE_URL` из вкладки "Variables"

#### Сервис 2: Redis

1. Нажмите "+ New" → "Database" → "Add Redis"
2. Дождитесь создания
3. **ВАЖНО:** Скопируйте `REDIS_URL` из вкладки "Variables"

#### Сервис 3: Backend API (main service)

1. Нажмите "+ New" → "GitHub Repo"
2. Выберите репозиторий: `afvayrapetyan-afk/Test-1`
3. Нажмите "Add variables" и добавьте:

```env
OPENAI_API_KEY=<ваш OpenAI ключ>
DATABASE_URL=<скопируйте из PostgreSQL сервиса>
REDIS_URL=<скопируйте из Redis сервиса>
CORS_ORIGINS=https://test-1-iota-sepia.vercel.app
PORT=8000
```

4. В Settings → Root Directory укажите: `backend`
5. В Settings → Start Command укажите:
   ```
   uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```

6. В Settings → Generate Domain - активируйте public URL
7. **ВАЖНО:** Скопируйте URL (например: `https://your-backend-production.up.railway.app`)

#### Сервис 4: Celery Worker

1. Нажмите "+ New" → "GitHub Repo"
2. Выберите тот же репозиторий: `afvayrapetyan-afk/Test-1`
3. Добавьте ТЕ ЖЕ переменные что и для Backend API
4. В Settings → Root Directory укажите: `backend`
5. В Settings → Start Command укажите:
   ```
   celery -A app.tasks.scheduled_tasks worker --loglevel=info
   ```

#### Сервис 5: Celery Beat (Планировщик - ГЛАВНЫЙ для автоматизации!)

1. Нажмите "+ New" → "GitHub Repo"
2. Выберите тот же репозиторий: `afvayrapetyan-afk/Test-1`
3. Добавьте ТЕ ЖЕ переменные что и для Backend API
4. В Settings → Root Directory укажите: `backend`
5. В Settings → Start Command укажите:
   ```
   celery -A app.tasks.scheduled_tasks beat --loglevel=info
   ```

---

## 🌐 Шаг 3: Обновите Frontend на Vercel (5 минут)

### 3.1 Войдите на Vercel

1. Откройте: https://vercel.com
2. Найдите проект "test-1"
3. Перейдите в Settings → Environment Variables

### 3.2 Обновите переменную

Найдите или создайте переменную:
```
Name: VITE_API_URL
Value: <URL вашего Railway Backend API из Шага 2.2.3 пункт 7>
```

Например: `https://your-backend-production.up.railway.app`

### 3.3 Redeploy

1. Перейдите в Deployments
2. Найдите последний deploy
3. Нажмите "..." → "Redeploy"
4. Дождитесь завершения

---

## ✅ Шаг 4: Проверка (5 минут)

### 4.1 Проверьте Backend

Откройте в браузере:
```
https://your-backend-production.up.railway.app/health
```

Должно показать: `{"status":"healthy"}`

### 4.2 Проверьте API с данными

```
https://your-backend-production.up.railway.app/api/v1/ideas/
```

Должны увидеть 10 идей на русском языке.

### 4.3 Проверьте Frontend

Откройте:
```
https://test-1-iota-sepia.vercel.app
```

Должны увидеть:
- ✅ 10 идей на главной странице
- ✅ Все тексты на русском
- ✅ Детальные страницы работают
- ✅ Метрики показываются

### 4.4 Проверьте Celery Beat (автоматизация)

1. Откройте Railway → Сервис "Celery Beat"
2. Перейдите в "Deployments" → View Logs
3. Должны увидеть что-то вроде:
   ```
   [2026-01-13 20:00:00,000: INFO] Scheduler: Sending due task discover-trends-daily
   ```

---

## 🎉 Готово!

Теперь ваша система:

✅ **Развернута на production**
- Backend: Railway (5 сервисов)
- Frontend: https://test-1-iota-sepia.vercel.app
- База данных: PostgreSQL на Railway
- Redis: Для Celery на Railway

✅ **Автоматически обновляется**
- Каждый день в 9:00 UTC
- Находит 10 новых трендов
- Генерирует 5+ бизнес-идей
- Всё переводит на русский через AI

✅ **Готова к показу**
- Постоянная ссылка: https://test-1-iota-sepia.vercel.app
- Никаких действий от вас не требуется
- Данные обновляются автоматически

---

## 🔧 Troubleshooting

### Проблема: Backend не запускается

**Решение:**
1. Проверьте логи в Railway → Backend API → Deployments → View Logs
2. Убедитесь что `OPENAI_API_KEY` правильный
3. Проверьте что `DATABASE_URL` и `REDIS_URL` корректные

### Проблема: Frontend показывает "Нет данных"

**Решение:**
1. Откройте DevTools (F12) → Network tab
2. Проверьте запросы к API
3. Убедитесь что `VITE_API_URL` в Vercel правильный
4. Проверьте что `CORS_ORIGINS` в Railway включает Vercel URL

### Проблема: Celery Beat не запускается автоматически

**Решение:**
1. Проверьте логи Celery Beat сервиса
2. Убедитесь что `REDIS_URL` одинаковый во всех 3 сервисах (Backend, Worker, Beat)
3. Перезапустите сервис Celery Beat

### Проблема: Идеи не обновляются в 9:00

**Решение:**
1. Проверьте что Celery Beat запущен (статус "Active")
2. Проверьте логи Celery Beat на ошибки
3. Проверьте баланс OpenAI API
4. Вручную запустите: `POST https://your-backend.railway.app/api/v1/agents/run`

---

## 💰 Стоимость

**Ежемесячная стоимость:**
- Railway: ~$5-10 (Hobby plan достаточно)
- Vercel: $0 (Hobby plan бесплатный)
- OpenAI API: ~$6-12 (автоматические обновления)

**Итого:** ~$11-22 в месяц

---

## 📞 Нужна помощь?

Если что-то не получается:
1. Проверьте логи на Railway
2. Проверьте DevTools в браузере
3. Прочитайте [DEPLOY_GUIDE.md](DEPLOY_GUIDE.md) для деталей
