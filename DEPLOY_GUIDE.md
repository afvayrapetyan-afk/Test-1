# 🚀 Руководство по Развертыванию

## Шаг 1: Развертывание Backend на Railway

### 1.1 Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите "Start a New Project"
3. Выберите "Deploy from GitHub repo"
4. Подключите ваш GitHub репозиторий
5. Выберите папку `backend` как root directory

### 1.2 Добавление сервисов

В вашем Railway проекте создайте следующие сервисы:

#### Сервис 1: PostgreSQL (или используйте SQLite)
- Нажмите "New" → "Database" → "Add PostgreSQL"
- Railway автоматически создаст `DATABASE_URL`

#### Сервис 2: Redis
- Нажмите "New" → "Database" → "Add Redis"
- Railway автоматически создаст `REDIS_URL`

#### Сервис 3: Backend API
- Это ваш основной backend сервис
- Variables (переменные окружения):
  ```
  OPENAI_API_KEY=<ваш ключ>
  DATABASE_URL=<из PostgreSQL сервиса>
  REDIS_URL=<из Redis сервиса>
  CORS_ORIGINS=https://test-1-iota-sepia.vercel.app
  PORT=8000
  ```
- Start Command: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

#### Сервис 4: Celery Worker
- Создайте еще один сервис из того же репозитория
- Используйте те же переменные окружения что и Backend API
- Start Command: `celery -A app.tasks.scheduled_tasks worker --loglevel=info`

#### Сервис 5: Celery Beat (Планировщик)
- Создайте еще один сервис из того же репозитория
- Используйте те же переменные окружения
- Start Command: `celery -A app.tasks.scheduled_tasks beat --loglevel=info`

### 1.3 Получение URL Backend

После развертывания Backend API:
1. Перейдите в сервис Backend API
2. Во вкладке "Settings" найдите "Public URL"
3. Скопируйте URL (например: `https://your-backend.railway.app`)

---

## Шаг 2: Развертывание Frontend на Vercel

### 2.1 Подготовка Frontend

Обновите файл `frontend/.env.production`:
```env
VITE_API_URL=https://your-backend.railway.app
```

### 2.2 Развертывание через Vercel Dashboard

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите "Add New" → "Project"
3. Выберите ваш GitHub репозиторий
4. Настройте проект:
   - **Framework Preset:** Vite
   - **Root Directory:** `frontend`
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Environment Variables:**
     ```
     VITE_API_URL=https://your-backend.railway.app
     ```

5. Нажмите "Deploy"

### 2.3 Настройка Custom Domain (опционально)

Если у вас есть домен:
1. Перейдите в Settings → Domains
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Vercel

---

## Шаг 3: Проверка Автоматического Обновления

### 3.1 Расписание

Система автоматически будет:
- **9:00 UTC каждый день** - Поиск 10 новых трендов
- **9:30 UTC каждый день** - Анализ новых трендов и генерация 5+ идей

### 3.2 Проверка логов

**Railway:**
1. Перейдите в сервис "Celery Beat"
2. Откройте вкладку "Deployments"
3. Проверьте логи на наличие:
   ```
   🔍 Starting automated trend discovery...
   💡 Starting automated idea analysis...
   ```

### 3.3 Ручной запуск (для тестирования)

Если хотите сразу добавить данные:

```bash
# Локально
cd backend
python demo.py
```

Или через Railway CLI:
```bash
railway run python demo.py
```

---

## Шаг 4: Мониторинг и Обслуживание

### 4.1 Проверка статуса сервисов

**Railway:**
- Backend API: https://your-backend.railway.app/health
- Проверьте что все 5 сервисов работают (зеленые галочки)

**Vercel:**
- Frontend: https://test-1-iota-sepia.vercel.app
- Проверьте что данные загружаются

### 4.2 Мониторинг затрат на OpenAI

**Каждый день:**
- Поиск трендов: ~$0.05-0.10
- Генерация идей: ~$0.15-0.30
- **Итого в месяц:** ~$6-12

### 4.3 Обновление кода

**Автоматическое развертывание:**
1. Сделайте `git push` в ваш репозиторий
2. Railway и Vercel автоматически задеплоят изменения

---

## Troubleshooting

### Проблема: Backend не запускается
- Проверьте переменные окружения (особенно `OPENAI_API_KEY`)
- Проверьте логи в Railway

### Проблема: Frontend не видит данные
- Проверьте `VITE_API_URL` в настройках Vercel
- Проверьте `CORS_ORIGINS` в backend переменных
- Откройте DevTools в браузере и проверьте Network tab

### Проблема: Celery задачи не выполняются
- Убедитесь что сервисы "Celery Worker" и "Celery Beat" запущены
- Проверьте что `REDIS_URL` одинаковый во всех сервисах
- Проверьте логи Celery Beat

### Проблема: Нет новых идей
- Проверьте что Celery Beat работает
- Проверьте логи на ошибки OpenAI API
- Убедитесь что есть непроанализированные тренды

---

## 🎉 Готово!

Теперь ваша система:
✅ Развернута на production серверах
✅ Доступна по постоянной ссылке
✅ Автоматически обновляется каждый день в 9:00
✅ Генерирует новые бизнес-идеи на основе трендов

**Ваша ссылка для показа:** https://test-1-iota-sepia.vercel.app
