# 🚀 Запуск AI Агентов - Финальный Шаг!

## ✅ Что уже готово:

- ✅ Backend сервер запущен на `http://localhost:8000`
- ✅ Frontend запущен на `http://localhost:3001`
- ✅ Интерфейс агентов доступен на `/agents`
- ✅ Python зависимости установлены
- ✅ .env файл создан

---

## 🔑 ПОСЛЕДНИЙ ШАГ: Добавь API Ключи

Сейчас в `.env` файле стоят placeholder ключи. Чтобы агенты **реально работали**, нужно добавить настоящие API ключи.

### 1. OpenAI API Key (для CodeAnalystAgent)

**Получить:**
1. Перейди: https://platform.openai.com/api-keys
2. Войди или создай аккаунт
3. Нажми **"Create new secret key"**
4. Скопируй ключ (начинается с `sk-proj-` или `sk-`)

**Добавить в .env:**
```bash
# Открой файл .env в корне проекта
# Найди строку:
OPENAI_API_KEY=sk-your-openai-api-key-here

# Замени на свой ключ:
OPENAI_API_KEY=sk-proj-твой-настоящий-ключ
```

### 2. Anthropic API Key (для DevAgent)

**Получить:**
1. Перейди: https://console.anthropic.com/
2. Войди или создай аккаунт
3. Settings → API Keys → **"Create Key"**
4. Скопируй ключ (начинается с `sk-ant-`)

**Добавить в .env:**
```bash
# В том же файле .env найди:
ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# Замени на свой ключ:
ANTHROPIC_API_KEY=sk-ant-твой-настоящий-ключ
```

### 3. Перезапустить Backend

После добавления ключей:

```bash
# Останови текущий backend (Ctrl+C в терминале где он запущен)

# ИЛИ найди процесс и убей его:
ps aux | grep "python3 main.py"
kill <process_id>

# Запусти заново:
cd backend
python3 main.py
```

---

## 🎯 Тестирование Агентов

### Способ 1: Через Интерфейс (Рекомендуется)

1. **Открой:** http://localhost:3001/login
2. **Войди:**
   - Логин: `Админ`
   - Пароль: `987654`

3. **Тестируй CodeAnalystAgent:**
   - Кликни на синюю карточку "CodeAnalystAgent"
   - Введи путь: `backend/main.py`
   - Нажми **[Анализ]**
   - Получишь: quality score, bugs, improvements

4. **Тестируй DevAgent:**
   - Кликни на фиолетовую карточку "DevAgent"
   - Введи описание: `Add logging to API endpoints`
   - Нажми **[Создать фичу]**
   - Получишь: созданную ветку, PR, измененные файлы

---

### Способ 2: Через Swagger UI

1. **Открой:** http://localhost:8000/docs
2. **Найди endpoint:** `/api/agents/code-analyst/analyze`
3. **Нажми "Try it out"**
4. **Введи:**
   ```json
   {
     "path": "backend/main.py"
   }
   ```
5. **Нажми "Execute"**
6. **Получишь** JSON с результатами анализа

---

### Способ 3: Через curl

```bash
# CodeAnalystAgent - Анализ
curl -X POST http://localhost:8000/api/agents/code-analyst/analyze \
  -H "Content-Type: application/json" \
  -d '{"path": "backend/main.py"}' | python3 -m json.tool

# CodeAnalystAgent - Поиск багов
curl -X POST http://localhost:8000/api/agents/code-analyst/find-bugs \
  -H "Content-Type: application/json" \
  -d '{"path": "backend/agents/dev_agent.py"}' | python3 -m json.tool

# DevAgent - Создать фичу
curl -X POST http://localhost:8000/api/agents/dev-agent/implement \
  -H "Content-Type: application/json" \
  -d '{
    "description": "Add rate limiting middleware",
    "create_pr": true
  }' | python3 -m json.tool
```

---

## 🔧 Проверка Статуса

### Проверить что backend работает:
```bash
curl http://localhost:8000/health
```

**Ожидаемый ответ:**
```json
{
  "status": "healthy",
  "github_configured": true,
  "openai_configured": true,
  "anthropic_configured": true
}
```

### Проверить доступные endpoints:
```bash
curl http://localhost:8000/docs
```
Откроется Swagger UI со всеми доступными API.

---

## ⚠️ Возможные Ошибки

### Ошибка: "Invalid API key"
**Решение:**
- Проверь что скопировал ключ полностью (без пробелов)
- Проверь что перезапустил backend после изменения .env
- Убедись что ключ активен в консоли OpenAI/Anthropic

### Ошибка: "Connection refused" или "CORS error"
**Решение:**
- Убедись что backend запущен (`http://localhost:8000/health` отвечает)
- Проверь что frontend на порту 3001 (`http://localhost:3001`)
- Проверь CORS настройки в `backend/main.py`

### Ошибка: "Rate limit exceeded"
**Решение:**
- Подожди 1 минуту
- Проверь лимиты в настройках аккаунта OpenAI/Anthropic
- Пополни баланс если нужно

---

## 💰 Стоимость

### Примерные расходы:
- **Анализ одного файла** (CodeAnalyst): $0.01
- **Создание фичи** (DevAgent): $0.05-0.15
- **100 запросов в день**: ~$5-10

### Рекомендации:
1. Установи лимиты в настройках OpenAI/Anthropic ($10-20 в месяц)
2. Мониторь использование:
   - OpenAI: https://platform.openai.com/usage
   - Anthropic: https://console.anthropic.com/settings/billing
3. Для тестирования $5-10 хватит на несколько дней

---

## 📊 Примеры Использования

### Пример 1: Проверить безопасность всех API endpoints

```bash
# Через интерфейс:
1. Открой /agents
2. Выбери CodeAnalyst
3. Введи: backend/api/agents.py
4. Нажми [Security]
5. Получишь список уязвимостей
```

### Пример 2: Создать middleware для логирования

```bash
# Через интерфейс:
1. Открой /agents
2. Выбери DevAgent
3. Введи:
   "Создать middleware для логирования всех HTTP запросов
    с timestamp, method, path, status code"
4. Нажми [Создать фичу]
5. Получишь PR с готовым кодом
```

### Пример 3: Найти все баги в сервисах

```bash
# Через curl:
for file in backend/services/*.py; do
  echo "Проверяю: $file"
  curl -X POST http://localhost:8000/api/agents/code-analyst/find-bugs \
    -H "Content-Type: application/json" \
    -d "{\"path\": \"$file\"}" | python3 -m json.tool
done
```

---

## ✅ Готово!

После добавления API ключей и перезапуска backend:

**Открой:**
```
http://localhost:3001/agents
```

**Логин:** Админ
**Пароль:** 987654

**И начни управлять AI агентами! 🤖✨**

---

## 📚 Дополнительная Документация

- **API Ключи:** [docs/API_KEYS_SETUP.md](./API_KEYS_SETUP.md)
- **Агенты (полная документация):** [docs/AGENTS_GUIDE.md](./AGENTS_GUIDE.md)
- **Сравнение агентов:** [docs/AGENTS_TABLE.md](./AGENTS_TABLE.md)
- **Готово к использованию:** [docs/READY_TO_USE.md](./READY_TO_USE.md)

---

**Вопросы? Проблемы? Проверь health endpoint и логи backend! 🚀**
